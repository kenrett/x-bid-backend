pre-commit:
  parallel: true
  commands:
    rubocop:
      run: bundle exec rubocop
      stage_fixed: true
    test:
      run: bin/test

pre-push:
  parallel: false
  commands:
    test:
      run: bin/test
