<table role="presentation" cellpadding="0" cellspacing="0" width="100%">
  <tr>
    <td style="padding:0; color:#f8fafc; font-size:16px;">
      <h1 style="margin:0 0 12px; font-size:24px; font-weight:700; color:#ffffff;">Reset your password</h1>
      <p style="margin:0 0 16px; line-height:1.6; color:#dfe3f0;">
        Hello <%= @user.name.presence || "there" %>, use the secure link below to set a new password for your X‑Bid account.
      </p>
      <table role="presentation" cellpadding="0" cellspacing="0" style="margin:0 0 16px;">
        <tr>
          <td align="center" style="border-radius:999px; background:linear-gradient(90deg, #ff69b4, #c43de3, #a020f0);">
            <a href="<%= @reset_url %>" style="display:inline-block; padding:14px 24px; color:#ffffff; text-decoration:none; font-weight:700; font-size:15px; border-radius:999px;">
              Reset password
            </a>
          </td>
        </tr>
      </table>
      <p style="margin:0 0 10px; line-height:1.6; color:#dfe3f0;">
        Or copy this link if the button doesn’t work:
      </p>
      <p style="margin:0 0 18px; word-break:break-all;">
        <a href="<%= @reset_url %>" style="color:#ff9ae0; text-decoration:none;"><%= @reset_url %></a>
      </p>
      <% unless Rails.env.production? %>
        <p style="margin:0 0 18px; color:#9ca3af; font-size:13px;">
          Dev/test token for convenience: <code style="background:#1f1f33; color:#f8fafc; padding:6px 8px; border-radius:8px; font-family:'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, monospace; font-size:12px;"><%= @reset_url.split("token=").last %></code>
        </p>
      <% end %>
      <p style="margin:0; line-height:1.6; color:#9ca3af; font-size:13px;">
        If you didn’t request this, you can safely ignore this email.
      </p>
    </td>
  </tr>
</table>
